[project]
name = "mlops-housing"
version = "0.1.0"
description = "MLOps challenge - housing price prediction"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
